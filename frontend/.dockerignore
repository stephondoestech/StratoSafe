# Exclude node_modules and build output (the build directory will be created during the build stage)
node_modules
build

# Exclude Git metadata and other local config files
.git
.gitignore

# Exclude local environment variables file (if not needed for the build)
.env

# Exclude files not needed in the container
Dockerfile
README.md
